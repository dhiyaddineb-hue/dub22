# FireRedTTS3 Arabic zero-shot voice cloning
# Install the official source from vendor/FireRedTTS3 first.
torch==2.8.0
torchaudio==2.8.0
torchcodec==0.7.0
transformers==5.6.2
einops==0.8.2
python-dotenv
regex
safetensors
